# AI Prompts Used

1. "Design a Cloudflare Agents application that includes Llama 3.3 (Workers AI), Workflows coordination, chat + voice input, and persistent memory/state. Provide a minimal architecture and file layout."
2. "Draft the Worker/Agent code to call `env.AI.run()` with Llama 3.3 and store chat history in durable state."
3. "Create a lightweight browser UI for chat + voice dictation that uses the Agents Client SDK and shows live memory updates."
4. "Write clear README instructions for running the app locally and deploying with Wrangler."
5. "Fix Cloudflare Agents `@callable()` decorator issues by aligning tsconfig with TC39 decorators."
6. "Improve the brief output rendering to display bold text and bullet lists instead of raw markdown."
7. "Add collapsible recent context UI to keep long conversations readable."
8. "Improve speech recognition UX for longer dictation and lock it to English only."
